<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>OTP verification</title>
</head>
<style>
    /* Result Skip Results Iframe */
.otp-field {
  flex-direction: row;
  column-gap: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.otp-field input {
  height: 45px;
  width: 70
  px;
  border-radius: 6px;
  outline: none;
  font-size: 1.125rem;
  text-align: center;
  border: 1px solid #ddd;
}
.otp-field input:focus {
  box-shadow: 0 1px 0 rgba(0, 0, 0, 0.1);
}
.otp-field input::-webkit-inner-spin-button,
.otp-field input::-webkit-outer-spin-button {
  display: none;
}

.resend {
  font-size: 12px;
}

.footer {
  position: absolute;
  bottom: 10px;
  right: 10px;
  color: black;
  font-size: 12px;
  text-align: right;
  font-family: monospace;
}

.footer a {
  color: black;
  text-decoration: none;
}
</style>
<body class="container-fluid bg-body-tertiary d-block">

  <div class="form-check d-flex justify-content-center text-danger">
    <%
    
    if(typeof message != 'undefined'){
            %>
            <h6>
                <%= message %>
            </h6>
            <%
    }

    %>
  </div>


    <div class="row justify-content-center">
      <div class="row justify-content-center">
        <div class="col-12 col-md-6 col-lg-4" style="min-width: 500px;">
          <div class="card bg-white mb-5 mt-5 border-0" style="box-shadow: 0 12px 15px rgba(0, 0, 0, 0.02);">
            <div class="card-body p-5 text-center">
              <h4>Verify</h4>
              <p>Your code was sent to you via email</p>
  
              <form action="/updatePassword" method="POST"> <!-- Add form element and set action and method -->
                <div class="otp-field mb-4">
                  <input type="number" name="otp" maxlength="6" required/>
                  <input type="hidden" name="email" value="<%=email%>">
                 
                </div>                                                                                                                                                                                      
  
                <button type="submit" class="btn btn-primary mb-3"> <!-- Change button type to submit -->
                  Verify
                </button>
              </form>
  
              <p class="resend text-muted mb-0">
                Didn't receive code? <a href="">Request again</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
</body>
</html>
